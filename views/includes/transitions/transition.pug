svg.spacer(viewbox='0 0 960 300' xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink' version='1.1')
    g.transition-1
        block path1
    g.transition-2(style="visibility: hidden;")
        block path2
        

script.
    for (let i = 1; i <= 7; i++) {
        console.log()
        KUTE.fromTo(
            `.transition-1-${i}`,
            { path: `.transition-1-${i}` },
            { path: `.transition-2-${i}` },
            { repeat: 9999, duration: 2000, yoyo: true, easing: 'easingSinusoidalInOut' }
        ).start();
    }